<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.whl.server.dao.whl.StudentDAO">

    <select id="getBillAmount">
        select yb.bill_amount billAmount from ysyy_bill yb
        where yb.bill_type IN ()
        <if test="">
            AND  yb.__id = #{orderId}
        </if>

    </select>

</mapper>
